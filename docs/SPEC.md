# SuperMeeting — 仕様・アイデア置き場（SPEC）

壁打ち（仕様検討）の成果を貯めるための**生きたドキュメント**です。
Chat で発散したアイデアや決めたことを、ここに書き出しておくと、
以降どの Claude Code セッションからも参照できます（会話は揮発、ファイルは永続）。

> - **恒久的な前提・アーキテクチャ**は `CLAUDE.md` を参照（重複させない）。
> - このファイルは「**何を作るか / なぜそう決めたか / これから何を検討するか**」を扱う。
> - 迷ったら追記優先。決まったら「決定ログ」へ、実装したら「現在の仕様」へ移す。

---

## 使い方（運用ルール）

1. 思いついたアイデアは **§バックログ** に1行足す（粗くてOK）。
2. 検討して方針が決まったら **§決定ログ** に「決定・理由・日付」を書く。
3. 実装が済んだら **§現在の仕様** に反映し、バックログから消す。
4. 判断に迷う点は **§未解決の論点** に置いて、壁打ちの起点にする。

---

## プロダクト概要 / ゴール

Google Meet などのオンライン会議で、**リアルタイムに議事ノートを画面共有してファシリテーション**する。
音声を書き起こし、AI が「要約 / 論点 / 決定事項 / TODO / 未解決の問い / 図示 / 調査・社内情報検索」に整理する。

- 想定ユーザー: 会議のファシリテーター
- 使い方の前提: ファシリが本アプリを画面共有しながら進行
- 提供価値: 議論の可視化・抜け漏れ防止・意思決定と宿題の明確化

---

## 現在の仕様（実装済み）

| 機能 | 現在の挙動 |
|---|---|
| 書き起こし | ブラウザ標準の Web Speech API（ja-JP / en-US 切替、自動再開）。手入力・サンプル投入も可 |
| 話者分離 | Chrome 拡張が Meet の発言者を検出し話者付与（自動＋手動固定）。拡張なしでも話者バーで手動切替 |
| 議事整理 | 現在の議論の要約 |
| 論点 | ステータス（未着手/議論中/結論あり）付きで一覧 |
| 決定事項 / TODO | 決定事項の抽出、TODO は担当者を可能な範囲で自動付与 |
| 図示 | Mermaid flowchart で議論構造を描画 |
| 調査 | Claude の web_search による Web 調査（要 APIキー、未設定はモック） |
| 社内情報検索 | プロバイダ抽象 + モック（実コネクタは未実装） |
| エンジン | APIキーがあれば Claude、無ければルールベース/モックに自動フォールバック |

---

## バックログ（検討中・未確定のアイデア）

> 状態: 💡アイデア / 🔍検討中 / ✅採用（→決定ログへ） / ❌見送り

| 状態 | アイデア | メモ |
|---|---|---|
| 💡 | 議事録エクスポート（Markdown / Slack 投稿 / Notion） | 会議後の価値に直結。まず Markdown が最小 |
| 💡 | 社内検索の実コネクタ（Slack / Drive / Confluence / Jira / Box） | OAuth 連携が必要。まず1つから |
| 💡 | 高精度クラウド STT（Whisper / Deepgram） | 日本語精度・同時発話耐性 |
| 💡 | 分析・調査のストリーミング表示 / 差分更新 | 体感速度の改善 |
| 💡 | Meet 拡張の自動話者検出セレクタを実 Meet で調整 | 手動固定はフォールバックとして動作済み |

<!-- 新しいアイデアはここに1行ずつ追記 -->

---

## 決定ログ（意思決定の記録）

> 形式: `YYYY-MM-DD 決定内容 — 理由`

- 2026-07-04 形態は Web アプリ（画面共有前提） — Meet 非依存で作りやすく汎用的
- 2026-07-04 書き起こしはブラウザ標準（後で API 差し替え可） — 無料・即動作、層分離で置換容易
- 2026-07-04 AI キーが無くても動く（heuristic / mock にフォールバック） — 出荷時から動くことを保証
- 2026-07-04 話者分離は Chrome 拡張ルート — 別タブの Web アプリからは Meet 内を覗けないため。手動固定を確実なフォールバックに
- 2026-07-04 調査は Claude web_search、社内検索はプロバイダ抽象＋モック — 実コネクタは認証が必要なため段階的に

<!-- 新しい決定はここに追記 -->

---

## 未解決の論点（壁打ちの起点）

- 議事録エクスポートの第一形態は? （Markdown ファイル DL / クリップボード / Slack 投稿 のどれを先に）
- 社内検索は最初にどのサービスを実装する? （利用頻度・認証の容易さで選ぶ）
- 高精度 STT に切り替える基準は? （Web Speech API の精度に不満が出たら / 特定言語 / コスト許容）
- 会議の「セッション」概念を持つ? （複数会議の保存・履歴・再開が必要か）

<!-- 新しい論点はここに追記 -->

---

## 用語

- **エンジン**: 分析を生成する主体。`claude`（AI）/ `heuristic`（ルールベース）/ `mock`（調査のダミー）
- **話者ラベル**: 発話に付く発言者名。AI へは「話者名: 発言」形式で渡す
